# Agent scaffolding (AGENTS.md, .agents/, .claude/, CLAUDE.md symlinks) is
# local-only and never tracked — see saya6k/hacs-wardrowbe for the same
# convention.
AGENTS.md
CLAUDE.md
.agents/
.claude/
.DS_Store
